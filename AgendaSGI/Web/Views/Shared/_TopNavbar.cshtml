@{ 
    Models.Usuarios usuario = (Models.Usuarios)HttpContext.Current.Session["Usuario"];

    string usuarioNombre = string.Empty;

    if (usuario != null)
    {
        usuarioNombre = usuario.StrNombre;
    }

}

<nav class="main-header navbar navbar-expand navbar-white navbar-light">
    <!-- Boton Menu -->
    <ul class="navbar-nav">
        <li class="nav-item">
            <a class="nav-link" data-widget="pushmenu" href="#" role="button"><i class="fas fa-bars"></i></a>
        </li>
    </ul>

    <!-- Boton lateral derecho -->
    <ul class="navbar-nav ml-auto">
        <li class="nav-item dropdown ">
            <a href="#" class="nav-link text-primary font-weight-bold" data-toggle="dropdown"><i class="fas fa-user mr-2"></i>@usuarioNombre<i class="fas fa-caret-down ml-1"></i></a>
            @*<a class="nav-link" data-toggle="dropdown" href="#">
                <i class="far fa-bell"></i>
                <span class="badge badge-warning navbar-badge">15</span>
            </a>*@
            <div class="dropdown-menu dropdown-menu-lg dropdown-menu-right">
                <a href="@Url.Action("Index","Cuenta")" class="dropdown-item text-primary font-weight-bold">
                    <i class="fas fa-user-cog mr-2"></i> Ir a Mi Perfil
                </a>
                <div class="dropdown-divider"></div>
                <a href="@Url.Action("CerrarSesion","Auth")" class="dropdown-item text-danger font-weight-bold">
                    <i class="fas fa-sign-out-alt mr-2"></i>Cerrar sesión
            
                </a>

        </div>
        </li>

   
        <!-- Notifications Dropdown Menu -->
      
        @*<li class="nav-item">
            <a class="nav-link" data-widget="control-sidebar" data-slide="true" href="#" role="button">
                <i class="fas fa-th-large"></i>
            </a>
        </li>*@
    </ul>
</nav>
